# Plan due-date support for the todo CLI

Inspect the current todo CLI fixture and write a short implementation plan in `DUE_DATE_PLAN.md`.

Do not implement the feature yet. The plan should cover:

- the command-line interface for adding an optional due date when creating a todo item
- how due dates should appear in `list` output
- validation rules for date input
- updates needed for tests or eval checks
- likely edge cases and a small implementation sequence

Keep the plan specific to this repository and the existing todo CLI task.
